# Secrets — ignore every .env variant EXCEPT the checked-in example
# (the bare `.env` pattern also matches per-bundle bundles/*/.env)
.env
.env.*
!.env.example

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# OS / editor
.DS_Store
*.swp
