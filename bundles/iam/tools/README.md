# IAM tools are code-backed

The IAM bundle registers its tools from `../pytools/` (see `iam_tools.py`):
`resolve-identity` and `secure-read-cypher`. Add static parameterized tools here
as `*.yaml` if you want them too.
