# SKELETON — implement the cypher: block. Test:
#   ACTIVE_BUNDLE=iam uv run python scripts/try_tool.py privilege_paths user_id=U-1002
name: privilege_paths
description: >
  Trace how a user reaches high-sensitivity resources — the access route(s) from
  the user through roles/permissions to any resource whose sensitivity is high or
  critical. Useful for blast-radius review of a compromised identity.
parameters:
  - name: user_id
    type: string
    description: The User.userId to analyse
    required: true
# HINT:
#   - Effective roles (direct + via group) -> ALLOWS -> Permission -> ON -> Resource.
#   - Keep resources WHERE res.sensitivity IN ['high','critical'].
#   - Returning the matched path (or its node names) makes the route easy to show.
cypher: |
  // TODO: implement privilege_paths. Params bind as $user_id. Keep it read-only.
  RETURN 'TODO: implement privilege_paths' AS todo
read_only: true
